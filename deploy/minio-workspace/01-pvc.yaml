apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-workspace-pvc
  namespace: swe-ai-fleet
  labels:
    app: minio-workspace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi
  storageClassName: local-path
