---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grpcui-orchestrator-ingress
  namespace: swe-ai-fleet
  labels:
    app: grpcui-orchestrator
    component: interactive-testing
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod-r53
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - grpcui-orchestrator.underpassai.com
    secretName: grpcui-orchestrator-tls
  rules:
  - host: grpcui-orchestrator.underpassai.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grpcui-orchestrator
            port:
              number: 80

