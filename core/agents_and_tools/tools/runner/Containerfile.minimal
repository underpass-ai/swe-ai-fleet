FROM ubuntu:24.04

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*

COPY agent-task /usr/local/bin/agent-task
RUN chmod +x /usr/local/bin/agent-task

CMD ["/usr/local/bin/agent-task"]

