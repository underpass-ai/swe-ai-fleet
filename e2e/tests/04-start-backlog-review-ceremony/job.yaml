apiVersion: batch/v1
kind: Job
metadata:
  name: e2e-start-backlog-review-ceremony
  namespace: swe-ai-fleet
  labels:
    app: e2e-start-backlog-review-ceremony
    version: v1.0.0
    test-type: e2e
    test-suite: e2e
spec:
  # Job configuration
  backoffLimit: 1  # Retry once on failure
  ttlSecondsAfterFinished: 60  # Auto-delete job 60 seconds after completion (success or failure)
  activeDeadlineSeconds: 600  # Maximum 10 minutes execution time (ceremony start may take time)

  template:
    metadata:
      labels:
        app: e2e-start-backlog-review-ceremony
        test-type: e2e
        test-suite: e2e
    spec:
      restartPolicy: Never

      containers:
      - name: test-runner
        image: registry.underpassai.com/swe-ai-fleet/e2e-start-backlog-review-ceremony:v1.0.0
        imagePullPolicy: Always

        # Environment variables for service discovery
        env:
        # Planning Service (gRPC)
        - name: PLANNING_SERVICE_URL
          value: "planning.swe-ai-fleet.svc.cluster.local:50054"

        # Test data
        - name: TEST_CREATED_BY
          value: "e2e-test@system.local"
        - name: TEST_PROJECT_NAME
          value: "Test de swe fleet"
        - name: TEST_EPIC_TITLE
          value: "Autenticacion"

        # Test execution options
        - name: PYTHONUNBUFFERED
          value: "1"

        # Resource limits
        resources:
          requests:
            cpu: "200m"
            memory: "512Mi"
          limits:
            cpu: "1000m"
            memory: "1Gi"

        # Security context
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL

      # DNS policy
      dnsPolicy: ClusterFirst
