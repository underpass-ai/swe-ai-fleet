# Dockerfile for Orchestrator E2E tests
FROM python:3.13-slim AS base

WORKDIR /workspace

# Install system dependencies
RUN --mount=type=cache,target=/var/cache/apt,sharing=locked \
    --mount=type=cache,target=/var/lib/apt,sharing=locked \
    apt-get update && \
    apt-get install -y --no-install-recommends \
    gcc \
    g++

# Install Python test dependencies
COPY tests/integration/services/orchestrator/requirements-test.txt /workspace/requirements-test.txt
RUN --mount=type=cache,target=/root/.cache/pip \
    pip install --no-cache-dir -r requirements-test.txt

# Copy proto specs (versioned API)
COPY specs/fleet/orchestrator/v1/orchestrator.proto /workspace/specs/fleet/orchestrator/v1/orchestrator.proto
COPY specs/fleet/ray_executor/v1/ray_executor.proto /workspace/specs/fleet/ray_executor/v1/ray_executor.proto

# Generate gRPC code
RUN mkdir -p /workspace/services/orchestrator/gen && \
    python -m grpc_tools.protoc \
    --proto_path=/workspace/specs/fleet/orchestrator/v1 \
    --proto_path=/workspace/specs/fleet/ray_executor/v1 \
    --python_out=/workspace/services/orchestrator/gen \
    --grpc_python_out=/workspace/services/orchestrator/gen \
    --pyi_out=/workspace/services/orchestrator/gen \
    /workspace/specs/fleet/orchestrator/v1/orchestrator.proto \
    /workspace/specs/fleet/ray_executor/v1/ray_executor.proto && \
    sed -i 's/^import orchestrator_pb2/from . import orchestrator_pb2/' \
    /workspace/services/orchestrator/gen/orchestrator_pb2_grpc.py && \
    sed -i 's/^import ray_executor_pb2/from . import ray_executor_pb2/' \
    /workspace/services/orchestrator/gen/ray_executor_pb2_grpc.py && \
    echo 'from . import orchestrator_pb2, orchestrator_pb2_grpc, ray_executor_pb2, ray_executor_pb2_grpc\n__all__ = ["orchestrator_pb2", "orchestrator_pb2_grpc", "ray_executor_pb2", "ray_executor_pb2_grpc"]' \
    > /workspace/services/orchestrator/gen/__init__.py

# Copy source code (needed for imports like swe_ai_fleet.*)
COPY src/ /workspace/src/

# Copy test files
COPY tests/integration/services/orchestrator/*.py /workspace/tests/integration/services/orchestrator/

# Copy pytest configuration
COPY pytest.ini /workspace/pytest.ini

# Set Python path
ENV PYTHONPATH=/workspace:/workspace/src

# Run tests
CMD ["pytest", "-m", "integration", "tests/integration/services/orchestrator/", "-v", "--tb=short"]

