# Environment Variables for SWE AI Fleet
# Copy this file to .env and fill in your values

# AWS Configuration (for Route53 DNS setup)
# Get your Hosted Zone ID from: AWS Console → Route53 → Hosted Zones
AWS_HOSTED_ZONE_ID=Z0XXXXXXXXXXXXXXXXX

# Optional: AWS credentials (if not using IAM roles or aws configure)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_DEFAULT_REGION=us-east-1

# Container Registry
# Default: registry.underpassai.com
# Change if using your own domain
# REGISTRY_DOMAIN=registry.yourdomain.com

# Application Domains (edit ingress YAMLs directly instead)
# UI_DOMAIN=swe-fleet.yourdomain.com
# RAY_DASHBOARD_DOMAIN=ray.yourdomain.com

# Kubernetes Context (if using multiple clusters)
# KUBECONFIG=/path/to/your/kubeconfig
# KUBE_CONTEXT=my-cluster-context
