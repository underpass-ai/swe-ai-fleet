# Ray Executor Service - FIXED VERSIONS
# Python 3.11 (matches Ray cluster and ray base images)
# NOTE: This service is the monorepo exception: it intentionally runs on Python 3.11.

# gRPC dependencies (pinned via root constraints)
grpcio
protobuf

# Ray for distributed agent execution (pinned via root constraints)
ray[default]

# NATS messaging (pinned via root constraints)
nats-py

# HTTP / async (pinned via root constraints)
aiohttp

# Logging and utilities (pinned via root constraints)
structlog
