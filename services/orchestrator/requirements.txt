# gRPC dependencies - FIXED VERSIONS (Python 3.13 compatible)
grpcio>=1.66.0
grpcio-tools>=1.66.0
protobuf>=5.26.0

# Core dependencies from main project - FIXED VERSIONS
redis==5.0.1
nats-py==2.10.0

# vLLM Agent dependencies - FIXED VERSIONS
aiohttp==3.12.14

# Ray for distributed agent execution - FIXED VERSION
ray[default]==2.49.2

# Logging and utilities - FIXED VERSIONS
structlog==23.2.0

