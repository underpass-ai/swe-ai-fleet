name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Lint
        run: echo "TODO: add ruff/flake8"
      - name: Unit tests
        run: echo "TODO: add pytest"
      - name: E2E (KinD)
        run: echo "TODO: kind cluster create && run smoke tests"
